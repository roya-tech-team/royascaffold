# Workflow Template

Use a `WF-*` artifact with: purpose/outcome, actors, trigger, preconditions, states or sequence, guards/authorization, data/contracts, side effects/events, failure/retry/timeout/compensation, idempotency, terminal outcomes, satisfying requirements, realizing components, and tests.

Add Mermaid only when it makes state or sequence materially clearer.

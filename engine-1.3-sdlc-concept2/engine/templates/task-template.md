# Execution Task Template

Task metadata follows `royascaff/task/v1`: ID/change/state/skill, required and optional artifact IDs, owned code paths, commands, constraints, outputs, context budgets, and dependencies.

Body: objective, acceptance subset, steps, file operations, evidence, escalation conditions. Another model must be able to execute it from its Context Pack without the original conversation.

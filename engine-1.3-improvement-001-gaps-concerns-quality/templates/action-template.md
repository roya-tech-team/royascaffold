# Action Catalog Template

For each `ACT-*` or preserved `EP-*`/`PG-*`/`VW-*`: trigger/kind, actor/auth, input/output contracts, called components, preconditions/invariants, success/failure/side effects, route/command/event/screen identity when applicable, idempotency/compatibility, tests, and standard metadata.


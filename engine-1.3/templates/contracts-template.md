# Contracts Template

For each `CTR-*` include kind, owner/version/compatibility, producers/consumers, fields/operations, validation, errors, sensitive classification, side effects/idempotency, mapping between domain/persistence/transport/integration/view models, implementations/actions, and contract tests.

Use for public/shared/versioned interfaces, requests/responses, events, provider payloads, CLI/public library APIs, configuration contracts.


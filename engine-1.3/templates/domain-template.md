# Domain Model Template

Include ubiquitous language, actors, concepts/entities/value objects, relationships, policies, invariants (`INV-*`), states/events, context/module boundaries, and mappings to requirements/workflows.

Conceptual meaning is independent from persistence/transport shape.


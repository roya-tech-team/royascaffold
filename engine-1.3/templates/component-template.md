# Component Catalog Template

For each `CMP-*` or preserved `SVC-*` include responsibility, kind/runtime/module/layer, public behavior, contracts/actions, dependencies and allowed direction, state/data/side effects, source owner, tests/operations links, exceptions/decision links, and standard metadata.

Private helpers attach using `supports`; do not create one component per file.


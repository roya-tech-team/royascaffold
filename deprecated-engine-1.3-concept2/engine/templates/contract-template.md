# Contract Template

Use `CTR-*` for transport APIs, DTOs, interfaces/ports, commands/queries, domain/integration events, provider payloads, configuration, and view models.

Record kind, version, owner, consumers, implementations, fields/operations, validation, errors, sensitivity, compatibility, mapping to domain/data/other contracts, and deprecation policy.

# Component Template

Use `CMP-*` for a runtime unit with independent responsibility: entry/transport, application/domain, persistence, integration, security, async, transformation, UI, or platform.

Record kind, responsibility, layer, contracts, workflow responsibility, dependencies, architecture rules/pattern roles, source through code map, tests, and status. A small helper attaches to an owner rather than becoming a component.

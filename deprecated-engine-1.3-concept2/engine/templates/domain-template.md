# Domain Template

Use `DOM-*` artifacts for concepts, value objects, policies, invariants, and domain events.

Record canonical term/aliases, meaning, owning context, relationships, invariants, lifecycle/events, mapped requirements, persistence projection if any, and enforcing components/tests. Do not define a business concept only as a database row.

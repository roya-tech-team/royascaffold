# System Map Template

Front matter: `document_id`, title, `layer: system-map`, schema version, status, owners.

Sections: purpose/outcomes; actors/external systems; system-context Mermaid; applications/runtimes; capability/module map; key workflows with `WF-*` links; architecture/deployment outline; quality/operations posture; current generated status link; next-layer reading links.

Target: 2–5k tokens. Summarize and link—do not duplicate layer content.


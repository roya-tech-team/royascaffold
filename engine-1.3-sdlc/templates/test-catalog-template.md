# Test and Evaluation Catalog Template

For each `TEST-*`: purpose/level, requirements/invariants/NFRs, target component/action/contract, method/command, environment/fixtures, expected result, evidence policy, owner, automation status, and standard metadata.

Do not mark implementation `verified` when required tests/evidence are missing.

